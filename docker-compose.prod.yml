name: "zematrack"

services:
  database:
    image: mongo:6.0
    container_name: mongodb
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - zematrack:/data/db

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    depends_on:
      - database
    restart: unless-stopped
    user: "mikiyas"

volumes:
  zematrack:
